// let lists = document.querySelectorAll("ul:not([class]):not([id])");
// lists = lists.splice(1, 23);
// let authors = [];
// Array.from(lists).forEach((list) => {
//   Array.from(list.children).forEach((li) => {
//     authors.push(li.querySelector("a")?.innerHTML);
//   });
// });
// //https://en.wikipedia.org/wiki/List_of_African-American_writers
